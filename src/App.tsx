import { RenderCanvas, RenderPipeline } from "render-composer";

function App() {
  return (
    <RenderCanvas>
      <RenderPipeline></RenderPipeline>
    </RenderCanvas>
  );
}

export default App;
